// backend — Backend API & Persistence
// Scope: Implements all HTTP API endpoints (GET /puzzle, POST /validate, POST /scores, GET /leaderboard), puzzle generation, validation logic, SQLite persistence, and leaderboard ordering. Owns the backend server and database.
// Owns: packages/backend/**
// This team builds its slice here each phase.
export {};
