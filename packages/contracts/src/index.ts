// contracts — Shared Contracts Package
// Scope: Defines all TypeScript interfaces and types for HTTP request/response payloads used between Backend and Frontend. Acts as the single source of truth for API contracts.
// Owns: packages/contracts/**
// This team builds its slice here each phase.
export {};
