// frontend — Frontend SPA
// Scope: Implements the browser-based SPA: interactive 9x9 Sudoku board, difficulty selection, puzzle fetching, result submission, leaderboard display, and optional client-side validation. Owns the web application.
// Owns: packages/web/**
// This team builds its slice here each phase.
export {};
